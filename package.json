{
  "name": "nostrcat",
  "version": "0.1.0",
  "description": "NoStrCat - Decentralized social platform combining Nostr protocol with OP_CAT Layer blockchain",
  "main": "dist/index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx scrypt-cli compile",
    "build:contracts": "npx scrypt-cli compile",
    "build:core": "cd packages/nostrcat-core && npm run build",
    "build:all": "npm run build:contracts && npm run build:core",
    "test": "mocha --require ts-node/register tests/**/*.test.ts",
    "test:contracts": "mocha --require ts-node/register tests/**/*.test.ts",
    "deploy": "npx tsx deploy.ts",
    "genprivkey": "npx tsx scripts/genPrivKey.ts",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist artifacts"
  },
  "dependencies": {
    "scrypt-ts": "^1.3.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/chai-as-promised": "^8.0.2",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "chai": "^4.3.0",
    "chai-as-promised": "^7.1.0",
    "eslint": "^8.0.0",
    "mocha": "^10.2.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "nostr",
    "bitcoin",
    "opcat",
    "social",
    "decentralized",
    "smart-contracts",
    "scrypt"
  ],
  "author": "NoStrCat Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nostrcat/nostrcat"
  }
}
